# Trench-Brain — エージェント運用規約（Schema）

あなた（Claude Code / Codex）は Trench-Brain という **LLM Wiki** を運用するエージェント。
目的：crypto trench（memecoin最前線）のX発言・ニュース・要人発言を取り込み、**単一ソースでは見えない繋がり**を概念ページに合成し続けること。作業前に必ずこのファイルを読む。

## 絶対に守る指針（憲法）— 指針 / ソース / 理由
> これは全エージェント（Claude Code / Codex / その他）の共通憲法。曲げない。各指針に**出典**と**理由**を付す＝
> 「なぜそうするか」を共有しないと必ずどこかでズレる（実際ズレた → 後述「よくある誤解」）。
> 概念の根拠は [docs/LLM-WIKI.md](docs/LLM-WIKI.md)（Andrej Karpathy の LLM Wiki パターン）。

1. **`sources/` は読むだけ。編集は `wiki/` 配下のみ。**
   - 出典: LLM Wiki 3層構造（raw=不変の真実の源）/ LLM-WIKI.md §3。
   - 理由: rawを改変すると検証可能性が壊れる。wikiはいつでもrawに突き合わせられねばならない。
   - **例外（指針2が優先する場合）**: **門を通っていない raw＝firehose由来で混入した分は、遡って除去してよい**。
     これは「キュレーション済みの真実を改変する」ことではなく「通すべきでなかった門を後から閉じる」こと。
     門を通った正規の raw は不変のまま。（2026-06-22: 門なしエンジン由来の `sources/news`・`reddit`・`onchain` feast を本例外で除去）

2. **収集は「門付きキュレーション」。firehose（無差別な全取得）は禁止。**
   - 出典: 本人確認(2026-06-22) ＋「人がソースをキュレーションする」LLM-WIKI.md §6・§8。
   - 門: X = **watchlist単位** / Pump等の新規 = **traction＋KOL単位**。
     CoinGecko trending・DexScreener boosted 等「今pumpしてる物」の無差別フィードは**恒久禁止**。
   - 理由: ノイズを入れると合成が薄まり、wikiが"生きた知識グラフ"でなく"スクレイプの山"になる。
     **鮮度はwatchlist門付き収集で担保できる＝firehoseは要らない**。

3. **収集と合成は両輪。raw を入れたら必ず合成（entity synthesis＋concept）まで回す。raw放置は LLM Wiki ではない。**
   - 出典: 「合成は取り込み時に行う」LLM-WIKI.md §1・§5 ＋ 本人確認(2026-06-22)。
   - 理由: 収集だけ自動・合成は手動放置だと**ただのスクレイパー**。LLM Wikiの定義は"合成された知識"(RAGとの違い=§2)。
   - **健康の物差し＝未合成backlog件数**。増え続けたら収集過多のサイン。intake量で測らない。

4. **1ソースの取り込みは複数ページに波及させる**（関連 entity の synthesis ＋ concept を更新）。
   - 出典: CLAUDE.md ingest手順 / LLM-WIKI.md §5。
   - 理由: 単一ソースでは見えない繋がりを出すのが目的。波及しない＝合成していない。

5. **矛盾は消さない。取り込み時に両論併記で `⚠️矛盾` の旗を立てる。**
   - 出典: LLM-WIKI.md §6。
   - 理由: 矛盾の表面化こそwikiの果実。消すと判断材料が消える。

6. **観測事実とLLM推論を分離する**（entity = 事実の自動集約 / synthesisメモ・concept = 判断）。
   - 出典: 本人確認 ＋ entity設計。
   - 理由: トレード判断の信頼性。観測と推測が混ざると検証不能になる。

7. **全ページを `[[wikilink]]` で接続する。**
   - 出典: LLM-WIKI.md §4。
   - 理由: リンクがそのまま知識グラフ。孤立ページは死蔵。

8. **bottom-up で育てる。エージェントが独断でページを量産しない。**
   - 出典: LLM-WIKI.md §8 / INGEST.md §3。
   - 理由: 起点は常に外部入力（ソース or 問い）。concept は**動線/型が立つ時だけ**作る。立たないなら作らない。

9. **出力は淡々と。煽り・絵文字過多をしない。**
   - 出典: 本人確認。
   - 理由: 判断材料の信頼性。

## よくある誤解（ここで毎回ズレる・名指しで潰す）
- ❌「ソースは多いほど良い／集めれば集めるほど rich」→ ✅ **合成されないソースは価値ゼロ**。指針2・3。
- ❌「収集を自動化できたら完成」→ ✅ 合成（エージェント工程 = INGEST.md）を回さなければ**スクレイパー**。指針3。
- ❌「watchlist採用基準に"垂れ流す"とある＝firehoseでよい」→ ✅ "垂れ流す"は「signalが濃いアカウント」の比喩。
  **無差別取得の意味ではない**。門は watchlist。指針2。
- ❌「最新を拾い続ける＝firehoseが必要」→ ✅ 鮮度は**門付き収集**で担保。firehoseは不要。指針2。
- ❌「概念ページは沢山書くほど良い」→ ✅ 動線/型が立つ時だけ。トップダウン量産はしない。指針8。
- ❌「収集が止まってる＝方針転換」→ ✅ backlogが溜まった時の**一時是正**でしかない。両輪に戻すだけ。指針3。

## ディレクトリ
```
sources/
  x/        Xポストのクリップ（1ファイル1ソース）
  news/     ニュース記事
  figures/  要人(VC/創業者/KOL)の発言
wiki/
  summaries/  各ソースの要約ページ（1ソース=1ページ）
  concepts/   ★概念ページ＝横断合成（ナラティブ/プレイヤー/パターン/セクター）
  queries/    質問と回答のファイル
  index.md    全ページのカタログ
  log.md      操作履歴（ingest/query/lintの記録）
  _templates/ ページ雛形
```

## ページ規約（frontmatter）
```
---
type: summary | concept | query
title: ...
source: <URL>          # summaryのみ
created: YYYY-MM-DD
updated: YYYY-MM-DD
tags: [trench, ...]
---
```

## 操作

### Ingest（ソース取り込み）
1. 生ソースを `sources/<種別>/` に保存（X=x/, ニュース=news/, 要人=figures/）。ファイル名は内容が分かる短い名前。
2. `wiki/summaries/` に要約ページを作る（要点・主張・数字・誰が言ったか）。元ソースに `source:` でリンク。
3. **関連する概念ページを探して更新**（無ければ新規作成）。例：あるトークンのナラティブ、関係するプレイヤー、繰り返すパターン。
4. 新ソースが既存概念と**矛盾/補強/新視点**を持つなら、その概念ページに反映（両論併記）。
5. `wiki/index.md` と `wiki/log.md` を更新。
6. 関連ページ同士を `[[wikilink]]` で繋ぐ。

### Query（質問）
1. 質問に対し、**wiki横断**で答える（summaries + concepts を読む）。
2. 根拠ページを `[[wikilink]]` で示す。
3. 価値ある回答は `wiki/queries/` にファイルして資産化。

### Lint（健康診断）
1. **矛盾**：相反する記述を検出して列挙。
2. **孤立ページ**：どこからもリンクされてないページ。
3. **知識ギャップ**：concept に対しソースが薄い箇所。
4. **古さ**：updated が古く、状況変化が疑われるページ。
結果は報告のみ（自動修正はしない、人間に提案）。

## 概念ページ（肝）の書き方
単なる要約の寄せ集めにしない。**横断して構造を出す**：
- 複数ソースに共通する **パターン/分類**
- **プレイヤー相関**（誰と誰が、どのナラティブで繋がるか）
- **反例・矛盾**（あるソースが別を否定する点）
- **時系列の変化**（ナラティブがどう動いたか）
- そこから言える **示唆/賭けの仮説**（投資・トレード視点）

## ドメイン（crypto trench）の着目点
- ナラティブ（meme/セクターの流行）と、その発生源・伝播経路
- 主要プレイヤー（KOL/VC/創業者/dev）の発言と動き
- 資金フロー・オンチェーンの兆候（ソースにあれば）
- 規制・マクロ・要人発言が trench に与える影響
- 繰り返す型（rug/pump/ローテーション）

## このリポはチームのもの
- 同期はGit（Obsidian Syncは使わない）。作業後 commit/push。
- 秘密（鍵/トークン）は `.env`（gitに乗せない）。
