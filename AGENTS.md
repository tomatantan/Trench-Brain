# AGENTS.md

このリポの運用規約は **[CLAUDE.md](./CLAUDE.md)** に集約。
Codex / その他エージェントも、作業前に CLAUDE.md を読んでそれに従うこと。

要約：
- Trench-Brain は crypto trench の LLM Wiki。
- `sources/` は読むだけ、編集は `wiki/` のみ。
- 操作は ingest / query / lint（`.claude/skills/` 参照）。
- 全ページ `[[wikilink]]` で接続。同期は Git。
